#include <stdio.h>
#include <math.h>
#include <stdlib.h>
typedef struct 
{
    double x,y; // positions
    double ax,ay; // acceleration
    double vx,vy; // velocity
    double m; //mass
    double F; //total force exertion

} planet;


int main(int argc, char *argv[]) {
    double N=atoi(argv[1]);
    int nsteps=atoi(argv[2]);
    int number_particles
    const float e=0.001,delta_t=0.00001;
    const double G=100/N;
    FILE* fp=fopen(filename,"rb");

    return 0;

}